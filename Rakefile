#!/usr/bin/rake

## [ Constants ] ##############################################################

WORKSPACE = 'SwiftGenKit'
TARGET_NAME = 'Tests'
CONFIGURATION = 'Debug'
POD_NAME = 'SwiftGenKit'
TEST_PATH = "Tests/#{POD_NAME}Tests"


task :default => 'xcode:test'
